/*
 * #include <complex>
 * #include <cstdarg>
 * #include <iomanip>
 * #include <iostream>
 * #include <sstream>
 *
 * #include <deque>
 * #include <map>
 * #include <typeindex>
 *
 * template <class A>
 * class T
 * { };
 * template <class B>
 * class U
 * { };
 *
 * typedef std::deque <T <int>> ComponentInfoArray;
 *
 * template <typename A, typename B>
 * constexpr auto
 * fun (A a, B b) -> decltype (a + b)
 * {
 * return a * b;
 * }
 *
 * auto a = fun (std::complex <double> (1), 2.0);
 *
 *
 * //char* s = R'This is a "raw" string!';
 *
 * //#include "Test.h"
 * //using namespace titania::Test;
 *
 *
 * int
 * main (int, char**)
 * {
 * std::clog << "Titania test started . . ." << std::endl;
 * std::clog << " Compiled at " << __DATE__ << " " << __TIME__ << std::endl;
 *
 * //benchmark ("t1", test1, 10000000);
 * //benchmark ("t2", test2, 1000000000);
 *
 *
 * //std::istringstream isstream ("1.3634  ,   2.3424 ,  ");
 * //isstream >> a;
 * //std::clog << "a: " << a << std::endl;
 * //std::clog << "i:" << isstream .fail () << isstream .rdbuf () << std::endl;
 *
 * std::clog << "### " << __TIME__ ": Titania done ..." << std::endl;
 * return 0;
 * }
 */
